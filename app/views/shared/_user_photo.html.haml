.row
  %aside.col-md-4
    %section.user_info
      %h3
        = image_tag(@user.avatar_url) if @user.avatar?
        = @user.name
.col-md-8
  - if @user.photos.any?
    %h3
      Фотографии пользователя (#{@user.photos_count})
    %ol.photos= render @photos
    = will_paginate @photos